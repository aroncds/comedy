{
    ; Pausable Modifiers
    (def 'ptr-paused 0x03)
    (def 'paused @@ptr-paused)
    (def 'when-not-paused
        (when (= paused 1) revert))
    (def 'set-paused (value) (sstore ptr-paused value))
}