{
    ; Owner modifiers
    (def 'owner 0x00) ; address
    (def 'get-owner @@owner)
    (def 'set-owner (address) (sstore 0x00 address))
    (def 'only-owner
        (unless (= (caller) get-owner) revert))
}